export function start(tabId) {
  console.log('[OVC] keyframe.start — implemented in Task 7');
}

export function markOut(tabId, outTime, inTime, url, title, platform, videoTitle, channel) {
  console.log('[OVC] keyframe.markOut — implemented in Task 7');
}
